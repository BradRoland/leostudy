Update the EXISTING spectator mode (already working) to add two improvements: 1) Spectators can see the live gameplay state of BOTH players (side-by-side) in realtime. 2) Both players see a subtle 👁 watcher count on their in-game HUD. DO NOT rebuild spectator from scratch — extend the current implementation. -------------------------------------------------------------------- A) SHOW BOTH PLAYERS' LIVE GAMEPLAY TO SPECTATORS -------------------------------------------------------------------- In the current Spectator View, add a "Dual Player Live View" section that displays both players at the same time. UI (desktop): - Two columns (left = Player 1, right = Player 2) UI (mobile): - Stacked cards (Player 1 above Player 2) Each player card must update in realtime and include: - display name (+ agency badge if available) - current round (1–5) - score summary: - quiz: total score + correct/wrong - matching: total time so far + mistakes (or whatever you already track) - status: "answering / locked / finished / disconnected" - optional: progress bar (0–5 rounds completed) Realtime data source: - Use the SAME existing room/game realtime state you already use to show spectator progress. - If current spectator view only loads one player, extend it to subscribe/read both players' states from the room (e.g., both rows in room_players, or whatever structure exists). - Ensure updates are efficient and do not create duplicate realtime subscriptions. Important: - Keep it "spectator-safe" consistent with your current approach: - If you currently hide question text/choices during the match, keep hiding them. - If you currently show them, don't change that behavior unless needed. - The requirement here is to show BOTH players' gameplay/progress, not necessarily full questions. -------------------------------------------------------------------- 😎 ADD WATCHER COUNT (👁) ON BOTH PLAYERS' HUD -------------------------------------------------------------------- Add a subtle watcher indicator in the in-game HUD for BOTH players (while playing), showing how many spectators are currently watching. UI: - Small eye icon + count, e.g. "👁 12" - Place near top HUD (room info area) and keep it subtle/professional. Data: - Use the SAME spectator tracking you already have working. - If you already track watchers/presence, expose a derived count and subscribe to changes in realtime. - If you don't currently have a reliable count but spectator works, add minimal counting: - When a spectator enters the room watch page, register presence (or insert/update a room_spectators record with last_seen_at). - Count watchers as active spectators within the last 30 seconds. - Update count in realtime for players (subscribe to presence changes or table changes). Behavior: - Count updates live as spectators join/leave. - If the spectator is one of the players (shouldn't happen in spectator mode), don't include them in the watcher count. -------------------------------------------------------------------- C) ACCEPTANCE CRITERIA -------------------------------------------------------------------- - Spectator view shows two live-updating player cards for BOTH players in the room. - The data is clearly visible and updates smoothly during gameplay. - Both players see "👁 {count}" on their HUD during the match. - Watcher count updates in realtime as spectators join/leave. - No regressions: existing spectator join/watch flow still works. - No duplicate subscriptions or memory leaks (unsubscribe on unmount). Implement this as a clean incremental change using the current room state + realtime wiring already in the codebase.